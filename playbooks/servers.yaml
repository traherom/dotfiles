- name: DNS/DHCP Server
  hosts: public
  tasks:
   - name: Ping my hosts
     ansible.builtin.ping:

  # Install dnsmasq and config files
  # Install nginx and config files
  # Install wireguard config files, enable service 
  # Allow incoming HTTP/HTTPS


- name: VM Host
  hosts: vm-host
  tasks:
   - name: Ping my hosts
     ansible.builtin.ping:

  # Install virt-manager or whatever the CLI tools for that come from
  # Allow incoming 5900-5910 ports

all:
  vars:
    ansible_user: traherom
    jetbrains_toolbox_version: "1.26.5.13419"
    state_storage_dir: /.ansible-state-tracking
    font:
      name: UbuntuMono
      version: v2.2.2
    remote_pets_path: "~{{ ansible_user }}/.config/home-pets"
    dotfiles_path: "{{ remote_pets_path }}/dotfiles"
    op:
      url: moreharts.1password.com
      email: ryan@moreharts.com
      secret: "{{ op_secret_key }}"

  children:
    development:
      hosts:
        devtop:
          ansible_host:
            devtop.home.moreharts.com
    # servers:
    #   hosts:
    #     public:
    #       ansible_host:
    #         home.moreharts.com
    #     vm-host:
    #       ansible_host:
    #         vm-host.home.moreharts.com

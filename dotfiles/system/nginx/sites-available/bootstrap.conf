# Simple template file for letting Certbot get the initial certificate
server {
    listen 80 default_server;
    listen [::]:80 default_server;
    server_name DOMAIN;

    server_tokens off;

    location /.well-known {
        alias /var/www/;
    }
}
